# Copy to .env.test.local and fill in. Gitignored via the existing
# .env*.local pattern.
#
# Used by Playwright happy-path specs that exercise real OpenRouter calls.
# Specs that don't need it (local-only smoke, settings shell, a11y) skip
# this entirely.
OPENROUTER_TEST_API_KEY=sk-or-...

// Loaded once for the apps/web project. Tightens TS lib defaults
// (e.g. JSON.parse → unknown, .filter(Boolean) narrows). See
// https://github.com/total-typescript/ts-reset
/// <reference types="@total-typescript/ts-reset" />

export {};

# Imports And Aliases

Prefer absolute imports when aliases are available.

## Web Aliases

- `@/*` -> `apps/web/src/*`
- `@shared/*` -> `packages/shared/src/*`
- `@convex/*` -> `packages/convex/convex/*`

Keep alias definitions aligned across tsconfig, Next bundler, and linting when enforced.
